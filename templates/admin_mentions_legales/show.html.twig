{% extends 'base-backend.html.twig' %}

{% block title %}Mentions Légales{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_mentions_legales_index') }}" class="btn btn-dark mb-4">Retour</a>
    <h1>Mentions Légales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mentions_legale.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mentions_legale.nom }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ mentions_legale.texte|raw }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex flex-wrap justify-content-between align-items-center">
        {{ include('admin_mentions_legales/_delete_form.html.twig') }}
        <a href="{{ path('app_admin_mentions_legales_edit', {'id': mentions_legale.id}) }}"class="btn btn-success"><i class="align-middle" data-feather="edit-2"></i></a>

    </div>
{% endblock %}

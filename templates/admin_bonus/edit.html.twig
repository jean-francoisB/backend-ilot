{% extends 'base-backend.html.twig' %}

{% block title %}Modification Bonus{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_bonus_index') }}" class="btn btn-dark mb-4">Retour</a>
    <div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modification Bonus</h1>
        {{ include('admin_bonus/_delete_form.html.twig') }}
    </div>
    {{ include('admin_bonus/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}

{% extends 'base-backend.html.twig' %}

{% block title %}Modification Accueil{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_home_index') }}" class="btn btn-dark mb-4">Retour</a>
    <div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modification Accueil</h1>
        {{ include('admin_home/_delete_form.html.twig') }}

    </div>

    {{ include('admin_home/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}

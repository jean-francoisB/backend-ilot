{% extends 'base-backend.html.twig' %}

{% block title %}Modification Sortie{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_sortie_index') }}" class="btn btn-dark mb-4">Retour</a>
    <div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modification Sortie</h1>
        {{ include('admin_sortie/_delete_form.html.twig') }}
    </div>
    {{ include('admin_sortie/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}
